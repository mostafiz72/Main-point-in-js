const person = {
    1: "John Doe",
    age: 30,
    city: "New York"
}

console.log(person[1]);  // amar object to emnitei access korte pari ekhon kono value jodi access korte na pari taile thard braket er mardhome access korte hobe.

function min(nums) { return Math.min(nums) }
console.log(min(1,3,2));